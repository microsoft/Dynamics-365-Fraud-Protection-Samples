@*
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.   
*@

@model IEnumerable<Contoso.FraudProtection.ApplicationCore.Entities.CatalogType>

@{
    ViewData["Title"] = "Types";
}

<div class="container">
    <div class="esh-orders">
        <article class="esh-orders-titles row">
            <section class="esh-orders-title col-xs-2">Types</section>
        </article>
        @if (Model != null && Model.Any())
        {
            @foreach (var item in Model)
            {
                <article class="esh-orders-items row">
                    <section class="esh-orders-item col-xs-2"> @Html.DisplayFor(modelItem => item.Type)</section>
                </article>
            }
        }
    </div>
</div>
